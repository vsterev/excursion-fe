# Пълен базов URL на API (с `/api`), същият модел като зад nginx
VITE_API_ORIGIN=http://localhost:4010/api

# Production URL of the frontend — used for canonical links, og:url and sitemap
# Dev: leave empty; Prod: https://trips.solvex.bg (no trailing slash)
VITE_SITE_URL=
